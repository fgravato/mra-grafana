apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: lookout
      defaultBucket: mra_events
    secureJsonData:
      token: your-secret-token